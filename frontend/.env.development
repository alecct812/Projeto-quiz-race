# Em desenvolvimento o front (Vite :5173) conecta no backend separado (:3001).
# Em produção (Render) esta variável NÃO existe, então o socket usa a mesma origem.
VITE_SERVER_URL=http://localhost:3001
